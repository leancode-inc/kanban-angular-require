define([],
    function () {
        'use strict';

        var UpdateBacklogController = function () {

        };

        UpdateBacklogController.$inject = [];
        return UpdateBacklogController;
    });
